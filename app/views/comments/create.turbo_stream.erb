<%= turbo_stream.remove "no_comments_message" %>
<%= turbo_stream.replace "new_comment_form", partial: "comments/form", locals: { post: @post, comment: @post.comments.build } %>
<%= turbo_stream.update dom_id(@post, :reply_count), pluralize(@post.reload.comments_count, "reply") %>
